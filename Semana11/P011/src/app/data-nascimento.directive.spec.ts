import { DataNascimentoDirective } from './data-nascimento.directive';

describe('DataNascimentoDirective', () => {
  it('should create an instance', () => {
    const directive = new DataNascimentoDirective();
    expect(directive).toBeTruthy();
  });
});
